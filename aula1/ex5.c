#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main () {

    char entrada[100];
    scanf("%s", entrada);
    int aux, i;
    int tam = strlen(entrada);

    for (i = 0; i < tam; i++) {
        if (entrada[i]) {

        }
    }

    printf("%s", entrada);

    return 1;
}
